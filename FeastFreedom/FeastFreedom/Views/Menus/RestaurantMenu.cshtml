@*@model IEnumerable<FeastFreedom.Models.Menu>*@
@model dynamic

@{
    ViewBag.Title = "RestaurantMenu";
    Layout = "_CustomLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <style>


        .footer {
            margin-top: 30px;
            position: fixed;
            left: 0;
            bottom: 0;
            width: 100%;
            text-align: center;
        }
    </style>
</head>
<body>

    <h2> @Model.KITCHEN.KitchenName Menu</h2>


    @*@using (Html.BeginForm("AddToCart", "Cart"))
        {
            <div class="pull-right">
                @Html.HiddenFor(Model.MENU.MenuItemID)
                @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
                <input type="submit" class="btn btn-default" value="Add to cart"/>
            </div>
        }*@

    <table class="table">
        <tr>
            <th>
                Menu Item
            </th>
            <th>
                Vegetrain
            </th>
            <th>
                Price
            </th>

            <th></th>
        </tr>

        @foreach (var item in Model.MENU)
        {
            <tr>
                <td>
                    @item.MenuItemName
                </td>
                <td>
                    @item.Veg_NoVeg
                </td>
                <td>
                    @item.MenuItemPrice
                </td>
                <td>
                    @*<button class="btn btn-default">Add</button>*@
                    @*<input type="submit" class="btn btn-default" value="Add to cart" />*@
                    @* @Url.Action("Add", "AddToCart", new { id = item.MenuItemID })*@

                    @*@Html.ActionLink("Add", "Add", "AddToCart", new { id = item.KitchenID }, null)*@


                    <a href="@Url.Action("Add", "AddToCart", new { id=item.MenuItemID, KitchenID = Model.KITCHEN.KitchenID })" class="btn btn-info btn-lg">
                        <span class="glyphicon glyphicon-shopping-cart" style="color:green"></span>Cart <span class="badge"></span>
                    </a>
                </td>
            </tr>
        }
    </table>

</body>

</html>